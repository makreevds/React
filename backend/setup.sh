#!/bin/bash

# Скрипт для установки виртуального окружения и зависимостей Django проекта
# Использование: ./setup.sh

set -e  # Остановка при ошибке

# Цвета для вывода
GREEN='\033[0;32m'
RED='\033[0;31m'
YELLOW='\033[1;33m'
NC='\033[0m' # No Color

echo -e "${YELLOW}🚀 Начинаю установку виртуального окружения...${NC}"

# Проверка наличия Python 3
if ! command -v python3 &> /dev/null; then
    echo -e "${RED}❌ Python 3 не найден. Установите Python 3.8 или выше.${NC}"
    exit 1
fi

PYTHON_VERSION=$(python3 --version | cut -d' ' -f2 | cut -d'.' -f1,2)
echo -e "${GREEN}✓ Python версия: $(python3 --version)${NC}"

# Проверка наличия pip
if ! command -v pip3 &> /dev/null; then
    echo -e "${RED}❌ pip3 не найден. Установите pip.${NC}"
    exit 1
fi

# Создание виртуального окружения
if [ -d "venv" ]; then
    echo -e "${YELLOW}⚠️  Виртуальное окружение уже существует. Удаляю старое...${NC}"
    rm -rf venv
fi

echo -e "${YELLOW}📦 Создаю виртуальное окружение...${NC}"
python3 -m venv venv

if [ ! -d "venv" ]; then
    echo -e "${RED}❌ Ошибка при создании виртуального окружения${NC}"
    exit 1
fi

echo -e "${GREEN}✓ Виртуальное окружение создано${NC}"

# Активация виртуального окружения
echo -e "${YELLOW}🔌 Активирую виртуальное окружение...${NC}"
source venv/bin/activate

# Обновление pip
echo -e "${YELLOW}⬆️  Обновляю pip...${NC}"
pip install --upgrade pip --quiet

# Установка зависимостей
echo -e "${YELLOW}📥 Устанавливаю зависимости из requirements.txt...${NC}"
if [ ! -f "requirements.txt" ]; then
    echo -e "${RED}❌ Файл requirements.txt не найден${NC}"
    exit 1
fi

pip install -r requirements.txt

echo -e "${GREEN}✓ Зависимости установлены${NC}"

# Выполнение миграций (опционально, можно закомментировать если БД ещё не настроена)
echo -e "${YELLOW}🗄️  Выполняю миграции базы данных...${NC}"
python manage.py migrate --noinput

echo -e "${GREEN}✓ Миграции выполнены${NC}"

echo -e "${GREEN}✅ Установка завершена успешно!${NC}"
echo -e "${YELLOW}💡 Для активации виртуального окружения выполните:${NC}"
echo -e "   ${GREEN}source venv/bin/activate${NC}"
echo -e "${YELLOW}💡 Для запуска сервера разработки:${NC}"
echo -e "   ${GREEN}python manage.py runserver${NC}"
echo -e "${YELLOW}💡 Для запуска через Gunicorn:${NC}"
echo -e "   ${GREEN}gunicorn config.wsgi:application --bind 127.0.0.1:8000${NC}"

